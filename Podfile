# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CustomNav' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CustomNav
  pod 'SnapKit', '~> 4.0.0'
  pod 'RxSwift', '~> 4.5'
  pod 'RxCocoa', '~> 4.5'
  pod 'NSObject+Rx'

  target 'CustomNavTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CustomNavUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
